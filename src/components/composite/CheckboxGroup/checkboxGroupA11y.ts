/**
 * CheckboxGroup uses native `<fieldset>` + `<legend>` for the accessible name.
 * Hint/error ids feed `Field.Set` → `aria-describedby`.
 */
export const CHECKBOX_GROUP_USES_NATIVE_FIELDSET = true as const;
