export function disclosureTriggerId(autoId: string): string {
  return `disclosure-trigger-${autoId}`;
}

export function disclosurePanelId(autoId: string): string {
  return `disclosure-panel-${autoId}`;
}
